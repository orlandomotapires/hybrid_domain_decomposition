#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
ROOT_DIR="$(cd "$SCRIPT_DIR/.." && pwd)"

cd "$ROOT_DIR/src"

if [[ -x "$ROOT_DIR/.env_ocean/bin/python" ]]; then
	exec "$ROOT_DIR/.env_ocean/bin/python" -m run_batch "$@"
fi

exec python3 -m run_batch "$@"